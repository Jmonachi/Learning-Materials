def sort(l):
    enum = []
    odd = []
    for n in l:
        if n % 2 == 0:
            enum.append(n)
    return enum

    for n in l:
    	if n %2 != 0:
    		odd.append(n)
    return odd
    		
l = [1, 2, 3, 4, 5, 6, 7, 8, 9]


print(odd)